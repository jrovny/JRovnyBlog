@model JRovnyBlog.Models.PostSummary

@* <div class="card h-100">
    <div class="row g-0">
        <div class="col-6 col-md-4 align-self-start py-3">
            <img src="@Model.Image" alt="Blog post image" class="img-fluid">
        </div>
        <div class="col-6 col-md-8 p-0">
            <div class="card-body pl-0 mr-1">
                <h5 class="card-title">
                    <a asp-page="/Posts/Index" asp-route-slug="@Model.Slug" class="card-link stretched-link fs-1">
                        @Model.Title
                    </a>
                </h5>
                <p class="card-text">
                    <small class="text-muted">@Model.CreatedDate.ToShortDateString() &bull; Jordan</small>
                </p>
            </div>
        </div>
    </div>
</div> *@
<div class="card h-100">
    <img src="@Model.Image" alt="Blog post image" class="img-fluid">
    <div class="card-body">
        <h5 class="card-title">
            <a asp-page="/Posts/Index" asp-route-slug="@Model.Slug" class="card-link stretched-link fs-1">
                @Model.Title
            </a>
            <p class="card-text">
                <small class="text-muted">@Model.CreatedDate.ToShortDateString() &bull; Jordan</small>
            </p>
        </h5>
    </div>
</div>